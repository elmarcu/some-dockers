{
    "CORSRules": [
        {
            "AllowedOrigins": ["*"],
            "AllowedMethods": ["GET", "HEAD"],
            "MaxAgeSeconds": 3000,
            "AllowedHeaders": ["Authorization"]
        }
    ]
}
